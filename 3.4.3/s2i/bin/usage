#!/bin/sh

DISTRO=`cat /etc/*-release | grep ^ID= | grep -Po '".*?"' | tr -d '"'`

cat <<EOF
This is a S2I R-3.4.3 ${DISTRO} base image:
To use it, install S2I: https://github.com/openshift/source-to-image

Sample invocation:

s2i build https://github.com/sclorg/s2i-R-container.git --context-dir=3.4.3/test/test-app/ ${NAMESPACE}/R-3.4.3-${DISTRO}7 R-sample-app

You can then run the resulting image via:
docker run -p 8080:8080 R-sample-app
EOF
